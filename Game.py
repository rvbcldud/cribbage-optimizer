class Game:
    def __init__(self, p1, p2, deck):
        self.p1 = p1
        self.p2 = p2
        self.deck = deck

    def play_turn(self):
        while len(self.p1.cards) > 0 and len(self.p1.cards) > 0:
            print("placeholder")
